#!/bin/bash
sketchybar \
  --set battery label.drawing=toggle \
  --set volume  label.drawing=toggle \
  --set vpn     label.drawing=toggle \
  --set wifi    label.drawing=toggle \
  --set ram     label.drawing=toggle \
  --set cpu     label.drawing=toggle \
  --set disk    label.drawing=toggle \
  --set network label.drawing=toggle
